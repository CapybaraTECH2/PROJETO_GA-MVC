{% extends "base.html.twig" %}

{% block main %}

<h1>Excluir Disciplina</h1>

<form action="/disciplina/delete/{{ disciplina.id }}" method="post">

    <div class="row">

        <h2 class="center" > 
            Confirmar Exclusão:
            <strong class="text-bg-danger px-3" >{{ disciplina.nome }}</strong>?

        </h2>

        <div class="my-3 col-sm-12 text-center">
            <button type="submit" class="btn btn-primary">Excluir</button>
        </div>
                
        </div>
  </form>
 {% endblock %}
